public class DiffiHallman implements Chipher {
    public byte[] encriptData(byte[] openText, byte[] key) {
        return new byte[0];
    }

    public byte[] decriptData(byte[] openText, byte[] key) {
        return new byte[0];
    }
}
